# platform/keycloak/overlays/dev/kustomization.yaml
#
# Overlay "dev" per Keycloak.
# Eredita da ../../base e applica eventuali patch o values specifici
# dell'ambiente di sviluppo.
#
# Stato attuale: PLACEHOLDER. Verra' popolato in Fase 3.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: platform-keycloak

resources:
  - ../../base
